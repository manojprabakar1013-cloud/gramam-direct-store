const products = [
  {id:1,name:'Cold-Pressed Groundnut Oil',category:'Oils',price:320,unit:'1 Litre',badge:'Available',image:'assets/images/groundnut-oil.svg',description:'Traditional wooden cold-pressed groundnut oil.',features:['Wooden cold-pressed','Traditional village method','1 litre pack','Available now']},
  {id:2,name:'Jackfruit',category:'Fruits',price:50,unit:'1 kg',badge:'Farm fresh',image:'assets/images/jackfruit.svg',description:'Fresh, honey-sweet jackfruit sourced directly from village orchards. Naturally aromatic with a soft, sweet texture.',features:['Naturally grown','Village orchard sourced','Sweet and aromatic','Available now']},
  {id:3,name:'Watermelon',category:'Fruits',price:30,unit:'1 kg',badge:'Summer fresh',image:'assets/images/watermelon.svg',description:'Crisp, sweet, and incredibly hydrating. Sourced fresh from village fields, our naturally grown watermelons are perfect to beat the heat and stay refreshed.',features:['Fresh from village fields','Naturally grown','Sweet and juicy','Available now']},
  {id:4,name:'Amla (Nellikai)',category:'Fruits',price:80,unit:'1 kg',badge:'Tangy fresh',image:'assets/images/amla.svg',description:'Traditional village-grown Indian gooseberries. These sour-tangy amlas are suitable for juices, pickles and traditional recipes.',features:['Village grown','Rich in Vitamin C','Perfect for juice and pickles','Available now']},
  {id:5,name:'Banana (Vazha Pazham)',category:'Fruits',price:60,unit:'1 Dozen (12 Pieces)',badge:'Naturally ripened',image:'assets/images/banana.svg',description:'Naturally ripened, premium local village bananas. Sweet, soft, and ideal for snacks, desserts and everyday use.',features:['Naturally ripened','Local village variety','Sweet and soft','Available now']},
  {id:6,name:'Drumstick (Murungakai)',category:'Vegetables',price:70,unit:'1 kg',badge:'Fresh harvest',image:'assets/images/drumstick.svg',description:'Fresh, tender, and highly aromatic village drumsticks. Perfect for adding authentic flavour to traditional sambar and aviyal.',features:['Fresh and tender','Rich in iron and vitamins','Ideal for sambar and aviyal','Available now']},
  {id:7,name:'Pomelo (Bablimass)',category:'Fruits',price:90,unit:'1 Piece (Approx. 1 kg)',badge:'Citrus fresh',image:'assets/images/pomelo.svg',description:'Freshly harvested citrus delight directly from rural orchards. With its unique mild sweet-tangy flavour, this juicy fruit is ideal for fresh servings and fruit salads.',features:['Rural orchard sourced','Sweet-tangy flavour','Rich in antioxidants','Available now']},
  {id:8,name:'Musk Melon (Kirnipazham)',category:'Fruits',price:50,unit:'1 kg',badge:'Summer favourite',image:'assets/images/muskmelon.svg',description:'Incredibly sweet and fragrant village musk melons. Ideal for thick summer milkshakes and fruit bowls.',features:['Fresh village produce','Sweet and fragrant','Ideal for shakes and fruit bowls','Available now']}
];

const money = n => `₹${Number(n).toLocaleString('en-IN')}`;
const cartKey='gramamDirectCart';
const orderKey='gramamDirectOrders';
const userKey='gramamDirectUser';
const whatsappNumber='918220272719';
const whatsappDisplay='+91 82202 72719';
const ordersApiUrl='https://script.google.com/macros/s/AKfycbyvS5ubSXImh711LFJewNoLLFPR2Lk5aFCQy4q-teDPUxHNQ4pYc32g2vLExsg5N3I-ow/exec';

function whatsappUrl(message){return `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`}
function openWhatsApp(message){window.open(whatsappUrl(message),'_blank','noopener')}
function productWhatsAppMessage(p,qty=1){const count=Math.max(1,Number(qty)||1);return `Hi Gramam Direct, I would like to order:\n\n${p.name} × ${count}\nQuantity: ${p.unit}\nPrice: ${money(p.price*count)}\n\nPlease confirm availability and delivery details.`}
function openProductWhatsApp(id){const p=products.find(x=>x.id===id);if(!p)return;const qty=document.querySelector('#detail-qty')?.value||1;openWhatsApp(productWhatsAppMessage(p,qty))}
function buildCartWhatsAppMessage(){const rows=cartRows();if(!rows.length)return 'Hi Gramam Direct, I would like to know more about your fresh village products.';const t=cartTotals();const items=rows.map((r,i)=>`${i+1}. ${r.product.name} × ${r.qty} (${r.product.unit}) - ${money(r.product.price*r.qty)}`).join('\n');return `Hi Gramam Direct, I would like to place this order:\n\n${items}\n\nSubtotal: ${money(t.subtotal)}\nShipping: ${t.shipping?money(t.shipping):'Free'}\nTotal: ${money(t.total)}\n\nPlease confirm availability and delivery details.`}
const getCart=()=>JSON.parse(localStorage.getItem(cartKey)||'[]');
const setCart=(cart)=>{localStorage.setItem(cartKey,JSON.stringify(cart));updateCartCount();};

function addToCart(id,qty=1){
  const cart=getCart();
  const row=cart.find(i=>i.id===id);
  if(row) row.qty+=Number(qty); else cart.push({id,qty:Number(qty)});
  setCart(cart);toast('Added to cart');
}
function removeFromCart(id){setCart(getCart().filter(i=>i.id!==id));renderCart();}
function updateQty(id,qty){const cart=getCart();const row=cart.find(i=>i.id===id);if(!row)return;row.qty=Math.max(1,Number(qty));setCart(cart);renderCart();}
function updateCartCount(){const el=document.querySelector('.cart-count');if(el)el.textContent=getCart().reduce((s,i)=>s+i.qty,0);}
function toast(msg){let t=document.querySelector('.toast');if(!t){t=document.createElement('div');t.className='toast';document.body.appendChild(t)}t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)}
function productCard(p){return `<article class="product-card">
  <a class="product-media" href="product.html?id=${p.id}"><img src="${p.image}" alt="${p.name}"><span class="product-badge">${p.badge}</span></a>
  <div class="product-info"><div class="product-category">${p.category}</div><a href="product.html?id=${p.id}" class="product-title">${p.name}</a><div class="product-bottom"><div><div class="price">${money(p.price)}</div><small>${p.unit}</small></div><button class="add-btn" onclick="addToCart(${p.id})" aria-label="Add ${p.name} to cart">+</button></div></div>
</article>`}
function renderFeatured(){const el=document.querySelector('#featured-products');if(el)el.innerHTML=products.slice(0,4).map(productCard).join('')}
function renderShop(){
  const grid=document.querySelector('#shop-grid');if(!grid)return;
  const search=document.querySelector('#search-products');const cat=document.querySelector('#category-filter');const sort=document.querySelector('#sort-products');
  function run(){let list=[...products];const q=(search?.value||'').toLowerCase();const c=cat?.value||'all';if(q)list=list.filter(p=>p.name.toLowerCase().includes(q)||p.category.toLowerCase().includes(q));if(c!=='all')list=list.filter(p=>p.category===c);if(sort?.value==='low')list.sort((a,b)=>a.price-b.price);if(sort?.value==='high')list.sort((a,b)=>b.price-a.price);grid.innerHTML=list.length?list.map(productCard).join(''):'<div class="empty-state">No products found.</div>'}
  [search,cat,sort].forEach(x=>x&&x.addEventListener('input',run));run();
}
function renderProduct(){
  const root=document.querySelector('#product-detail');if(!root)return;const id=Number(new URLSearchParams(location.search).get('id')||1);const p=products.find(x=>x.id===id)||products[0];document.title=`${p.name} | Gramam Direct`;
  root.innerHTML=`<div class="product-gallery"><img src="${p.image}" alt="${p.name}"></div><div class="product-copy"><span class="eyebrow">${p.category}</span><h1>${p.name}</h1><div class="rating">   🌿 Fresh village-sourced product </div><div class="detail-price">${money(p.price)} <small style="font-size:.9rem;color:var(--muted)">/ ${p.unit}</small></div><p>${p.description}</p><ul class="detail-list">${p.features.map(f=>`<li>${f}</li>`).join('')}</ul><div class="quantity-row" style="flex-wrap:wrap"><div class="quantity"><button onclick="changeDetailQty(-1)">−</button><input id="detail-qty" value="1" inputmode="numeric"><button onclick="changeDetailQty(1)">+</button></div><button class="btn btn-primary" onclick="addToCart(${p.id},document.querySelector('#detail-qty').value)">Add to cart</button><button class="btn" style="background:#25D366;color:#fff;box-shadow:0 10px 24px rgba(37,211,102,.22)" onclick="openProductWhatsApp(${p.id})">💬 Order on WhatsApp</button></div><div class="trust-card" style="margin-top:1rem"><strong>🚚 Free delivery on ₹999 and above</strong><p>Secure ordering • Carefully packed • Easy support</p></div></div>`;
  const related=document.querySelector('#related-products');if(related)related.innerHTML=products.filter(x=>x.id!==p.id).slice(0,4).map(productCard).join('')
}
function changeDetailQty(delta){const el=document.querySelector('#detail-qty');el.value=Math.max(1,Number(el.value||1)+delta)}
function cartRows(){return getCart().map(row=>({...row,product:products.find(p=>p.id===row.id)})).filter(x=>x.product)}
function cartTotals(){const subtotal=cartRows().reduce((s,r)=>s+r.product.price*r.qty,0);const shipping=subtotal===0?0:subtotal>=999?0:79;return{subtotal,shipping,total:subtotal+shipping}}
function renderCart(){
  const list=document.querySelector('#cart-items');if(!list)return;const rows=cartRows();
  list.innerHTML=rows.length?rows.map(r=>`<div class="cart-item"><img src="${r.product.image}" alt="${r.product.name}"><div><a href="product.html?id=${r.product.id}"><strong>${r.product.name}</strong></a><div style="color:var(--muted)">${r.product.unit}</div><div class="quantity" style="margin-top:.65rem;width:max-content"><button onclick="updateQty(${r.id},${r.qty-1})">−</button><input value="${r.qty}" onchange="updateQty(${r.id},this.value)"><button onclick="updateQty(${r.id},${r.qty+1})">+</button></div></div><div style="text-align:right"><div class="price">${money(r.product.price*r.qty)}</div><button onclick="removeFromCart(${r.id})" style="border:0;background:none;color:var(--terracotta);cursor:pointer;margin-top:.8rem">Remove</button></div></div>`).join(''):'<div class="empty-state"><h3>Your cart is empty</h3><p>Add village-made favourites from our shop.</p><a class="btn btn-primary" href="shop.html">Start shopping</a></div>';
  const t=cartTotals();['subtotal','shipping','total'].forEach(k=>{const el=document.querySelector(`#cart-${k}`);if(el)el.textContent=money(t[k])});const btn=document.querySelector('#checkout-btn');if(btn)btn.style.pointerEvents=rows.length?'auto':'none';
  const summary=document.querySelector('.cart-summary');let wa=document.querySelector('#whatsapp-cart-btn');if(summary&&!wa){summary.insertAdjacentHTML('beforeend','<button id="whatsapp-cart-btn" class="btn" style="width:100%;margin-top:.75rem;background:#25D366;color:#fff">💬 Order cart on WhatsApp</button>');wa=document.querySelector('#whatsapp-cart-btn');wa?.addEventListener('click',()=>openWhatsApp(buildCartWhatsAppMessage()))}if(wa){wa.disabled=!rows.length;wa.style.opacity=rows.length?'1':'.55';wa.style.cursor=rows.length?'pointer':'not-allowed'}
}
function renderCheckout(){const sum=document.querySelector('#checkout-summary');if(!sum)return;const rows=cartRows(),t=cartTotals();sum.innerHTML=rows.map(r=>`<div class="summary-line"><span>${r.product.name} × ${r.qty}</span><strong>${money(r.product.price*r.qty)}</strong></div>`).join('')+`<div class="summary-line"><span>Shipping</span><strong>${t.shipping?money(t.shipping):'Free'}</strong></div><div class="summary-line total"><span>Total</span><span>${money(t.total)}</span></div>`}
async function placeOrder(e) {   e.preventDefault();    const formEl = e.target;   const rows = cartRows();    if (!rows.length) {     toast("Your cart is empty");     return;   }    const form = new FormData(formEl);   const totals = cartTotals();    const orderId =     "GD" + Date.now().toString().slice(-8);    const paymentMethod =     String(       form.get("payment") ||       "Cash on Delivery"     );    const submitBtn = formEl.querySelector(     'button[type="submit"], button:not([type])'   );    const originalText =     submitBtn?.textContent || "Place order";    const payload = {     orderId: orderId,     customerName: form.get("name") || "",     phone: form.get("phone") || "",     email: form.get("email") || "",     address: form.get("address") || "",     city: form.get("city") || "",     pin: form.get("pin") || "",      products: rows.map(function (row) {       return {         name: row.product.name,         qty: row.qty,         unit: row.product.unit,         price: row.product.price,         lineTotal:           row.product.price * row.qty       };     }),      subtotal: totals.subtotal,     shipping: totals.shipping,     total: totals.total,     paymentMethod: paymentMethod,     orderStatus: "New"   };     if (paymentMethod === "Razorpay") {     if (submitBtn) {       submitBtn.disabled = true;       submitBtn.textContent =         "Opening secure payment...";     }      try {       localStorage.setItem(         "gramamDirectPendingPayment",         JSON.stringify({           id: orderId,           date: new Date().toLocaleDateString(             "en-IN"           ),           status: "Paid",           total: totals.total,           items: rows.map(function (row) {             return {               name: row.product.name,               qty: row.qty             };           }),           customer:             payload.customerName         })       );        const paymentForm =         document.createElement("form");        paymentForm.method = "POST";       paymentForm.action = ordersApiUrl;       paymentForm.style.display = "none";        const paymentFields = {         action:           "startRazorpayPaymentPage",          orderId:           payload.orderId,          customerName:           payload.customerName,          phone:           payload.phone,          email:           payload.email,          address:           payload.address,          city:           payload.city,          pin:           payload.pin,          products:           JSON.stringify(payload.products)       };        Object.entries(paymentFields)         .forEach(function ([name, value]) {           const input =             document.createElement("input");            input.type = "hidden";           input.name = name;           input.value = value;            paymentForm.appendChild(input);         });        document.body.appendChild(         paymentForm       );        paymentForm.submit();      } catch (error) {       console.error(         "Unable to open Razorpay",         error       );        localStorage.removeItem(         "gramamDirectPendingPayment"       );        toast(         "Unable to open online payment."       );        if (submitBtn) {         submitBtn.disabled = false;         submitBtn.textContent =           originalText;       }     }      return;   }     if (submitBtn) {     submitBtn.disabled = true;     submitBtn.textContent =       "Saving order...";   }    try {     await fetch(       ordersApiUrl,       {         method: "POST",         mode: "no-cors",         headers: {           "Content-Type":             "text/plain;charset=utf-8"         },         body: JSON.stringify(payload)       }     );      const order = {       id: orderId,       date:         new Date().toLocaleDateString(           "en-IN"         ),       status: "Confirmed",       total: totals.total,       items: rows.map(function (row) {         return {           name: row.product.name,           qty: row.qty         };       }),       customer:         payload.customerName     };      const orders = JSON.parse(       localStorage.getItem(orderKey) ||       "[]"     );      orders.unshift(order);      localStorage.setItem(       orderKey,       JSON.stringify(orders)     );      localStorage.removeItem(cartKey);      location.href =       "orders.html?placed=" +       encodeURIComponent(orderId);    } catch (error) {     console.error(       "Order save failed",       error     );      toast(       "Could not save order. Please try WhatsApp order."     );      if (submitBtn) {       submitBtn.disabled = false;       submitBtn.textContent =         originalText;     }   } }
function handleAuth(e,type){
  e.preventDefault();
  const f=new FormData(e.target);
  const user={
    name:f.get('name')||'Gramam Customer',
    email:f.get('email')
  };
  localStorage.setItem(userKey,JSON.stringify(user));
  toast(type==='register'?'Account created':'Welcome back');
  setTimeout(()=>location.href='dashboard.html',600);
}
function renderUser(){const u=JSON.parse(localStorage.getItem(userKey)||'null');document.querySelectorAll('[data-user-name]').forEach(el=>el.textContent=u?.name||'Guest User');document.querySelectorAll('[data-user-email]').forEach(el=>el.textContent=u?.email||'guest@example.com')}
function renderOrders() {   const root =     document.querySelector("#orders-body");    if (!root) return;    let orders = JSON.parse(     localStorage.getItem(orderKey) || "[]"   );    const params =     new URLSearchParams(location.search);    const placed =     params.get("placed");    const pendingPayment =     JSON.parse(       localStorage.getItem(         "gramamDirectPendingPayment"       ) || "null"     );    if (     placed &&     pendingPayment &&     pendingPayment.id === placed   ) {     const alreadyAdded =       orders.some(function (order) {         return order.id === placed;       });      if (!alreadyAdded) {       pendingPayment.status = "Paid";       orders.unshift(pendingPayment);        localStorage.setItem(         orderKey,         JSON.stringify(orders)       );     }      localStorage.removeItem(cartKey);      localStorage.removeItem(       "gramamDirectPendingPayment"     );      updateCartCount();      toast(       "Payment successful! Order " +       placed +       " confirmed"     );   } else if (placed) {     toast(       "Order " +       placed +       " placed successfully"     );   }    root.innerHTML = orders.length     ? orders.map(function (order) {         return `           <tr>             <td>               <strong>${order.id}</strong>             </td>             <td>${order.date}</td>             <td>               ${order.items.map(function (item) {                 return (                   item.name +                   " × " +                   item.qty                 );               }).join(", ")}             </td>             <td>${money(order.total)}</td>             <td>               <span class="status">                 ${order.status}               </span>             </td>           </tr>         `;       }).join("")     : `       <tr>         <td colspan="5">           <div class="empty-state">             No orders yet.             <a               href="shop.html"               style="color:var(--green);font-weight:800"             >               Start shopping             </a>           </div>         </td>       </tr>     `; }
function renderDashboard() {
  const orders = JSON.parse(
    localStorage.getItem(orderKey) || "[]"
  );

  const total = orders.reduce(function (sum, order) {
    return sum + Number(order.total || 0);
  }, 0);

  const values = {
    "dash-orders": orders.length,
    "dash-spent": money(total),
    "dash-pending": orders.filter(function (order) {
      return order.status !== "Delivered";
    }).length
  };

  Object.entries(values).forEach(function ([id, value]) {
    const element = document.getElementById(id);

    if (element) {
      element.textContent = value;
    }
  });
}function bindNewsletter(){document.querySelectorAll('.newsletter-form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();toast('Thank you for subscribing!');f.reset()}))}
function bindContact() {   const form = document.querySelector('#contact-form');    if (!form) return;    form.addEventListener('submit', function (event) {     event.preventDefault();      const data = new FormData(form);      const name = String(data.get('name') || '').trim();     const email = String(data.get('email') || '').trim();     const subject = String(data.get('subject') || '').trim();     const message = String(data.get('message') || '').trim();      const whatsappMessage =       `Hi Gramam Direct,

` +       `Name: ${name}
` +       `Email: ${email}
` +       `Subject: ${subject}

` +       `Message:
${message}`;      openWhatsApp(whatsappMessage);     toast('Opening WhatsApp...');   }); }
function navInit(){const btn=document.querySelector('.mobile-toggle'),links=document.querySelector('.nav-links');btn&&btn.addEventListener('click',()=>links.classList.toggle('open'))}
function commonHeader(active=''){return `<div class="announcement">Free delivery on orders of ₹999 and above • Direct from trusted village producers</div><header class="site-header"><div class="container nav"><a class="brand" href="index.html"><span class="brand-mark"><svg viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M13 37c10-2 16-8 19-19 7 9 12 14 20 18-7 9-18 14-31 14-4 0-7-5-8-13Z" fill="currentColor"/><path d="M31 17v32" stroke="#D8C2AE" stroke-width="4" stroke-linecap="round"/></svg></span><span>Gramam Direct</span></a><nav class="nav-links"><a class="${active==='home'?'active':''}" href="index.html">Home</a><a class="${active==='shop'?'active':''}" href="shop.html">Shop</a><a class="${active==='about'?'active':''}" href="about.html">Our Story</a><a class="${active==='contact'?'active':''}" href="contact.html">Contact</a></nav><div class="nav-actions"><a class="icon-btn account-link" href="orders.html" aria-label="My Orders">📦</a><a class="icon-btn" href="cart.html" aria-label="Cart">🛒<span class="cart-count">0</span></a><button class="icon-btn mobile-toggle" aria-label="Menu">☰</button></div></div></header>`}
function commonFooter(){return `<footer class="site-footer"><div class="container"><div class="footer-grid"><div><div class="brand" style="color:white"><span class="brand-mark">🌿</span><span>Gramam Direct</span></div><p>Village goodness, honestly sourced and thoughtfully delivered to your doorstep.</p></div><div><h4>Shop</h4><a href="shop.html">All products</a><a href="shop.html">Fresh fruits</a><a href="shop.html">Vegetables</a><a href="shop.html">Cold-pressed oils</a></div><div><h4>Company</h4><a href="about.html">Our story</a><a href="contact.html">Contact us</a><a href="orders.html">My orders</a></div><div><h4>Help</h4><a href="shipping.html">Shipping Policy</a><a href="refund.html">Cancellation & Refund</a><a href="privacy.html">Privacy Policy</a><a href="terms.html">Terms & Conditions</a><a href="${whatsappUrl('Hi Gramam Direct, I would like to know more about your products.')}" target="_blank" rel="noopener">WhatsApp: ${whatsappDisplay}</a></div></div><div class="footer-bottom"><span>© 2026 Gramam Direct. All rights reserved.</span><span>Made with care for local producers.</span></div></div></footer>`}
function injectWhatsAppButton(){if(document.querySelector('#whatsapp-float'))return;const a=document.createElement('a');a.id='whatsapp-float';a.href=whatsappUrl(buildCartWhatsAppMessage());a.target='_blank';a.rel='noopener';a.setAttribute('aria-label','Order on WhatsApp');a.style.cssText='position:fixed;left:18px;bottom:18px;z-index:99;display:flex;align-items:center;gap:.5rem;background:#25D366;color:#fff;padding:.8rem 1rem;border-radius:999px;font-weight:850;box-shadow:0 12px 30px rgba(20,90,50,.28);text-decoration:none';a.innerHTML='<span style="font-size:1.25rem">💬</span><span>WhatsApp Order</span>';a.addEventListener('click',()=>{a.href=whatsappUrl(buildCartWhatsAppMessage())});document.body.appendChild(a)}
function injectLayout(){const header=document.querySelector('[data-header]');const footer=document.querySelector('[data-footer]');if(header)header.innerHTML=commonHeader(header.dataset.header);if(footer)footer.innerHTML=commonFooter();injectWhatsAppButton()}

document.addEventListener('DOMContentLoaded',()=>{injectLayout();navInit();updateCartCount();renderFeatured();renderShop();renderProduct();renderCart();renderCheckout();renderUser();renderOrders();renderDashboard();bindNewsletter();bindContact()});
